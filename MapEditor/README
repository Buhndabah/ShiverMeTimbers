DEPENDANCIES: Python 2.X, Pygame, NumPy

w  = up one half tile
s  = down one half tile
a  = left one half tile
d  = right one half tile

up/down/left/right = shift view

x = next tile
z = previous tile
t = go up one layer
y = go down one layer

enter = place tile
backspace = delete tile

c = toggle collidable on map tile (not the tile tied to the cursor)
m = print map to file

escape = quit
